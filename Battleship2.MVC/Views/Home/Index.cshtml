@{
    ViewData["Title"] = "Home Page";
}

    <div class="text-center">
        <button type="button" class="btn btn-primary btn-lg" onclick="window.location.href='@Url.Action("Index", "Statistics")';">Game Statistics</button>
        @if (User.Identity.IsAuthenticated)
    {
        <button type="button" class="btn btn-primary btn-lg" onclick="window.location.href='@Url.Action("CreateGame", "Game")';">Start Game</button>
        <button type="button" class="btn btn-primary btn-lg" onclick="window.location.href='@Url.Action("ConnectToGame", "Game")';">Connect to Game</button>
    }
    </div>
